node_modules/
dist/

# Generated data — raw downloads (regenerable via `npm run fetch`) and built artefacts
# (regenerable via `npm run build:data`). Uploaded to S3 by the Build Data workflow,
# never committed: the artefacts are tens of MB each.
data/*
