{
  "name": "mvd-map",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Architectural atlas of Montevideo — citywide parcel and height data (POT), with Centro's curated heritage-protection inventory as a documented subset",
  "scripts": {
    "fetch": "node scripts/fetch.mjs",
    "build:data": "node scripts/build.mjs",
    "build": "node scripts/bundle.mjs",
    "dev": "node scripts/bundle.mjs --serve",
    "test": "node --test \"tests/*.test.mjs\""
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "polygon-clipping": "^0.15.7",
    "proj4": "^2.11.0",
    "shapefile": "^0.6.6",
    "topojson-server": "^3.0.1",
    "topojson-simplify": "^3.0.3"
  },
  "dependencies": {
    "d3": "^7.9.0",
    "rbush": "^4.0.1",
    "topojson-client": "^3.1.0"
  }
}
