{
  "name": "mvd-map",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Architectural atlas of Centro, Montevideo — parcels drawn from Intendencia vector data",
  "scripts": {
    "fetch": "node scripts/fetch.mjs",
    "build:data": "node scripts/build.mjs",
    "build": "node scripts/bundle.mjs",
    "dev": "node scripts/bundle.mjs --serve",
    "test": "node --test \"tests/*.test.mjs\""
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "proj4": "^2.11.0",
    "shapefile": "^0.6.6",
    "topojson-server": "^3.0.1",
    "topojson-simplify": "^3.0.3"
  },
  "dependencies": {
    "d3": "^7.9.0",
    "rbush": "^4.0.1",
    "topojson-client": "^3.1.0"
  }
}
